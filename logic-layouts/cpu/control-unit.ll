inputs 6

# CU는 다음과 같은 아웃풋 핀을 가지고 있다 ACCout, ACCin, TEMPout, TEMPin, ALUout, IRin, MARin, MEMin, MEMout, PCin, PCout
# ACCin = ACC가 인풋을 받을 것인지 아닌지 결정
# ACCout = ACC가 아웃풋을 돌려줄 것인지 아닌지 결정
# TEMPin = TEMP가 인풋을 받을 것인지 아닌지 결정
#          일반적으로 TEMP는 MEM의 데이터가 저장되어 ALU로 들어가기 위한 버퍼
# TEMPout = TEMP가 아웃풋을 돌려줄 것인지 아닌지 결정
# ALUout = ALU의 계산 결과를 돌려줄 것인지 아닌지 결정

